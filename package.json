{
  "name": "react-express-starter",
  "version": "1.0.0",
  "engines": {
    "node": ">=16.x"
  },
  "description": "Barebones full stack webapp starter with 0 frotnend vulnerabilities as of July 2021. React/Redux/Express/Sequelize",
  "main": "index.js",
  "scripts": {
    "heroku-postbuild": "npm run build --prefix webapp",
    "install": "npm --prefix backend install backend && npm --prefix webapp install webapp",
    "sequelize": "npm run --prefix backend sequelize",
    "sequelize-cli": "npm run --prefix backend sequelize-cli",
    "start": "npm start --prefix backend"
  },
  "eslintConfig": {
    "rules": {
      "no-extend-native": "off"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
